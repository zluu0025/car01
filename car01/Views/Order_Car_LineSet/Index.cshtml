@model IEnumerable<car01.Models.Order_Car_LineSet>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CarsSet.Car_Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrdersSet.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order_dates)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CarsSet.Car_Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrdersSet.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order_dates)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
